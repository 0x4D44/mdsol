# Scoring and Undo System Documentation

## Overview

This document provides comprehensive technical documentation for the Scoring and Undo/Redo systems in the Solitaire application.
The implementation spans `engine.rs` (game logic) and `main.rs` (UI and state management).

---

## 1. Scoring System Architecture

### Core Data Structures

**GameState (engine.rs:136-148)**
```rust
pub struct GameState {
    pub score: i32,              // Line 142: Current score
    pub moves: u32,              // Line 143: Move counter
    pub scoring_mode: ScoringMode, // Line 145: Standard or Vegas
    pub timed_scoring: bool,     // Line 146: Timer enabled flag
    pub vegas_cumulative: bool,  // Line 147: Cumulative Vegas mode
    // ... other fields
}
```

**ScoringMode Enum (engine.rs:129-133)**
```rust
pub enum ScoringMode {
    Standard,  // Traditional scoring with various point values
    Vegas,     // Casino-style: -$52 buy-in, +$5 per foundation card
}
```

**WindowState Timer Fields (main.rs:532-534)**
```rust
game_timer_active: bool,     // Line 532: Timer running flag
game_seconds: u32,           // Line 533: Elapsed time in seconds
timed_penalty_acc: u32,      // Line 534: Accumulator for 10-second penalty intervals
```

### Score Initialization

**deal_with_seed (engine.rs:240-257)**
```rust
fn deal_with_seed(&mut self, draw_mode: DrawMode, seed: u64) -> Result<()> {
    // Lines 246-256: Initialize score based on scoring mode
    match self.scoring_mode {
        ScoringMode::Standard => {
            self.score = 0;  // Line 248: Always reset to 0
        }
        ScoringMode::Vegas => {
            if self.vegas_cumulative {
                self.score -= 52;  // Line 252: Subtract from existing bank
            } else {
                self.score = -52;  // Line 254: Reset to -52
            }
        }
    }
    self.moves = 0;  // Line 258: Reset move counter
    // ...
}
```

---

## 2. Standard Scoring Mode

### Point Values

| Action | Points | Implementation Line |
|--------|--------|---------------------|
| Waste to Tableau | +5 | engine.rs:355 |
| Waste to Foundation | +10 | engine.rs:594 |
| Tableau to Foundation | +10 | engine.rs:594 |
| Flip tableau card | +5 | engine.rs:316, 445 |
| Foundation to Tableau | -15 | NOT IMPLEMENTED |
| Recycle waste | -100 | NOT IMPLEMENTED |
| Timer penalty | -2 per 10s | main.rs:730 |
